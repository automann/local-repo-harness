[install]
minimumReleaseAge = 259200
minimumReleaseAgeExcludes = ["local-repo-harness", "@types/bun"]

[test]
root = "tests"
timeout = 60000
preload = ["./tests/setup-timeout.ts"]
pathIgnorePatterns = ["_ref/**", "_ops/**", ".codegraph/**", ".understand-anything/**"]
