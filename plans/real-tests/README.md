# Real Test Plans

Generated by the improve skill on 2026-06-14. These plans cover black-box
installation testing for project-scoped `repo-harness` adoption after the v0.5
command-boundary sync.

Execute plans in order. Each executor must read the plan fully before starting,
honor its STOP conditions, write all run artifacts under ignored
`dev-tests/runs/`, and update this index row when done.

## Execution Order and Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Re-run v0.5 project-scoped real install acceptance | P0 | L | none | DONE: PASS in `dev-tests/runs/20260614-222135-v05-real-install-codegraph-isolation` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED
(with rationale).

## Coverage Matrix

| Objective | Evidence in Plan 001 |
|-----------|----------------------|
| Project-scoped install does not fall back to user-level state | Fake HOME plus real HOME no-write snapshots; Profiles A-E assert no user hooks, user skills, user MCP config, user `.repo-harness`, or global CodeGraph fallback. |
| Installed project workflow functions after adoption | Profile B verifies project adapters/runtime/repo-harness skills; Profile C verifies Waza/Mermaid project skills; Profile D verifies project CodeGraph index and MCP; Profile E verifies all-enabled repeated adoption, hook smoke, diagnostics, workflow checks, and target tests. |
| v0.5 command boundary is honored | All positive repo-local paths use `repo-harness adopt`; negative checks prove `update --repo`, `adopt --configure-codegraph`, and user-scope project runtime are rejected. |
| External skills/tools are usable without user scope | Waza/Mermaid are installed with `--copy` and no `-g`; CodeGraph is resolved from target `node_modules/.bin` with global fallback disabled; brain stays manifest-only. |

## Notes

- `plans/real-tests/` is the tracked planning surface.
- `dev-tests/` remains ignored and is the only place for real-test run
  artifacts, tarballs, temporary homes, target repositories, and reports.
- Do not treat older `dev-tests/runs/*repo-harness-0.4.3.tgz` results as
  current acceptance. They are useful only as historical fixture patterns.
